# Claude Code instructions

This repository shares one set of agent instructions across tools. The
authoritative guidance lives in the AGENTS.md files (root + per-app/per-package);
the import below pulls the root in so Claude Code reads the same rules as the
other agents. Add Claude-specific notes below the import if ever needed.

@AGENTS.md
