services:
  web:
    container_name: turborepo-starter-web
    image: web:latest
    build:
      context: .
      dockerfile: ./apps/web/Dockerfile
      # args:
      # CLOUD_RUN_API_SERVICE_URL: "http://host.docker.internal:5002"
    env_file:
      - ./apps/web/.env
    environment:
      - PORT=3000
      - HOSTNAME="0.0.0.0"
      - NODE_ENV=production
      - BASE_URL=http://nginx
    stop_signal: SIGINT
    ports:
      - "3000:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - app-network

networks:
  app-network:
    external: true
